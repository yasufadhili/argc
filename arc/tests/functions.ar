

// No return type

def one() {

}


// Normal return (Single type)
def two() i32 {}


// Returns a list of types (). Use std::array o two type identifiers (Maybe ) or something better

def three() (i8, u16) {
}


// They can also take params.

def add(a i64, b i64) i64 {}

// Incase has no params

def some {}

// For now just add support for this but it won't be used mainly or may be at all, cos it doesn't really make sense

def several_returns(a u8, b i64, time i32) (i8, u32, i16) {}

